# 2026-06-01 — System Initialization

## Phase 0: System Build

**Built today (by Hermes/Opus 4.8):**
- Verified environment: git 2.54, tmux 3.3.5, DeepSeek key (works), Telegram bot
  @Basilclaw_ai_bot (connected, chat 880315854).
- Found gh CLI at `C:\Program Files\GitHub CLI\gh.exe` — installed but NOT authenticated.
- Set git global identity: "Basil Al Shukaili".
- Scaffolded second-brain vault on Desktop (00-System .. 99-Inbox).
- Wired DeepSeek as delegation executor: delegation.model=deepseek-chat,
  provider=deepseek, base_url=https://api.deepseek.com. Guardrails: max_iterations=30,
  max_concurrent_children=3, child_timeout=600s.
- Stored DEEPSEEK_API_KEY + TELEGRAM_BOT_TOKEN in Hermes .env (out of chat/logs).
- Authored 4 core skills: github-reputation-orchestrator, github-quality-gated-pr,
  github-repo-triage, second-brain-dreaming.
- Wrote roadmap, guardrails, current target (NousResearch/hermes-agent), repo template.

**Blocker:** gh CLI not authenticated. Cannot open issues/push PRs until Basil runs
`gh auth login`. Reported to Telegram.

**Decisions:**
- First target = NousResearch/hermes-agent (deep familiarity, high value, dogfooding,
  strong Windows-quirks niche).
- Quality gate: DeepSeek does mechanical work only; Opus reviews every diff + writes all prose.
- Human-in-loop: none (Basil's instruction) — act freely, report on Telegram.

**Security note:** Basil pasted DeepSeek key + Telegram token in chat. Advised rotation.

**Next:** Once gh authenticated → triage NousResearch/hermes-agent, find first PR.


---

## Update: Second brain pushed to GitHub + model strategy revised

- Created PRIVATE repo: basilalshukaili/github-reputation-engine, pushed all state.
  Both laptops (Hermes here, Claude Code on laptop B) now work from the same repo.
- Added CLAUDE.md (auto-loaded by Claude Code), README, LICENSE, .gitignore, and portable
  agent-agnostic playbooks (04-Playbooks) converted from the Hermes skills.
- MODEL STRATEGY revised after inspecting the nous catalog:
  - Opus 4.8 is $5/$25 (cost-efficient tier; the -fast variant is the pricey one).
  - Whole catalog available on nous. Switched delegation.model from DeepSeek to
    anthropic/claude-sonnet-4.6 ($3/$15) — strong coding model, 5x cheaper than Opus,
    same provider, no separate key. DeepSeek key retained but not used.
  - Model ladder: Opus (brain) / Sonnet 4.6 (worker) / Haiku 4.5 (scout).
- REJECTED caveman repo: it cuts tokens by writing terse/broken prose — would make PRs and
  comments read as AI slop and damage reputation. Token savings via model routing instead.
- Contribution cron updated to pull/push the second-brain repo each run (multi-laptop sync).
- Triage finding: first-PR candidate #36771 already had 2 open PRs -> skipped. Lesson recorded.

Obsidian repos (kepano/obsidian-skills, obsidian-second-brain, obsidian-mind): used as DESIGN
references for vault structure + the dreaming concept, NOT installed (they bolt onto Claude Code
what Hermes has natively; plain Markdown is the portable choice that also works in Claude Code).


---

## Update: Full roadmap + cost/failover wired

- Researched reputation-building (GitHub community #170732, dblock's maintainer-growth recipe,
  CMU fake-stars study). Key: reputation = earned trust made visible, via 3 flywheels
  (contribution -> audience -> creation). NEVER fake signals (detected, destroys credibility).
- Rewrote 00-System/roadmap.md as a 5-phase long journey: Foundation -> Profile storefront ->
  Credibility via contributions -> Breadth+depth + reviewing -> Audience engine (content) ->
  Launch our own project (only after trust+audience exist).
- Added 00-System/cost-and-failover.md documenting the model ladder + failover design.
- COST/FAILOVER wired (Opus credits limited ~$7, no top-up here):
  1. Provider fallback chain set: Opus 4.8 (nous) -> deepseek-chat (deepseek) on rate-limit/5xx.
  2. "Save Progress" no_agent cron (every 30m): pure git commit+push of the second brain.
     Invokes NO LLM (cannot create content or burn credits), silent when nothing to save,
     refuses to commit anything secret-looking. Tested: works (exit 0, pushed).
- This satisfies Basil's rule: DeepSeek only saves/pushes progress, never creates new work at
  low quality. When Opus runs out, no work is lost; resume on Claude Code via the repo.

Note: Nous account is funded (not blocked) — earlier "blocker" was only gh auth, now fixed.
