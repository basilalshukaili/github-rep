# 2026-07-02

## 🏆 WIN: PR #43293 MERGED into NousResearch/hermes-agent
- Cherry-picked onto main as `72f75f845` **with authorship preserved** (kshitijk4poor: "Salvaged onto current main and merged"). Reviewer liuhao1024: "Correct fix for a long-standing compression budget undercount."
- That's **merged contribution #2** on hermes-agent (after #38832/#41717). Rapport strategy working.

## Fable 5 session — verification discipline kills a bad PR (again)
- Target was the June-scouted cache TTL-downgrade bug. On TODAY's main it **does not reproduce**: the Anthropic-path clobber was refactored away upstream; the remaining injections are Qwen-Portal-only (`_qwen_prepare_chat_messages*`, gated on `portal.qwen.ai`), where the caching policy never marks → nothing to clobber, and the injections are the deliberate sole marker source. Sending `ttl: "1h"` there is unverifiable (could 400). **Reverted the drafted fix — no PR.** Second save of the #42643 discipline.
- **Lesson reinforced:** bench intel expires FAST in this repo (~3 weeks = #43k→#57k). Re-verify every stale finding against current main before writing a line.
- June bench is dead: #42776 fixed (PR #42889); #42483 has 2 competing PRs (joining = spam).

## New target: #56747 — Windows console-window flashes (filed today, unclaimed)
- Maintainer priority #2 (cross-platform); umbrella #54220 (CLOSED, but contains a forensic map of leaking spawn sites + reverted-attempt warnings) — flashes persist per today's report.
- Our edge: native Windows machine → can reproduce/verify. Fix primitive exists: `hermes_cli/_subprocess_compat.py::windows_hide_flags()`.
- Sonnet audit agent sweeping current main for still-leaking spawn sites (Python `subprocess.*` without creationflags; TS `spawn` without `windowsHide`), cross-checked against the #54220 map + reverted attempts + open-PR conflicts.

## Model stack
- Fable 5 (`claude-fable-5`, $10/$50) added as Apex tier earlier today; running this session on it. Verification depth is visibly better — caught the method-boundary misread (injection sites were in `_qwen_prepare_chat_messages`, not `_prepare_anthropic_messages_for_api`) before it poisoned the PR narrative.

## Next
- Review audit → fix remaining leak sites with the repo's own compat helpers → verify on this Windows machine → PR referencing #56747 + the #54220 map.
- Nudge nothing; let merged momentum speak.
