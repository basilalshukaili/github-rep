# 2026-06-10

## Win in progress: opened a real PR on NousResearch/hermes-agent

- **PR #43293** — `fix(compressor): count tool_call envelope in tail-budget token estimate (#28053)`
  https://github.com/NousResearch/hermes-agent/pull/43293
- **The bug:** the context-compressor's tail-protection budget walks estimated assistant-message tokens from `content + function.arguments` only, dropping each tool_call's `id`/`type`/`function.name` + JSON structure → **2-15x undercount** on parallel-tool turns → protected tail overshoots budget → compression runs ~40% effective instead of ~80% → context bloats. P2 bug (#28053), open + unassigned.
- **The fix:** consolidated 3 duplicated buggy walks (`_prune_old_tool_results` + 2 passes in `_find_tail_cut_by_tokens`) into one `_estimate_msg_budget_tokens()` helper counting the full envelope via `len(str(tc))`. Net −22/+18 in source. Cleaner than the abandoned prior attempt #28074 (author-closed, not rejected — I acknowledged it in the PR).
- **Tested on native Windows (py3.11):** new `tests/agent/test_compressor_tool_call_budget.py` + existing compression suite = **209 passed**.

## How this one was chosen (process that worked)
- Pulled full upstream source; read README/CONTRIBUTING/AGENTS.md → learned the maintainer ranks bug/cross-platform/security/perf ABOVE new features. So "wow" = a verifiable correctness/token win, not a shiny feature.
- 3 scout agents (caching / compression / maintainer-wants). **Verified every top lead against real code before building** (the #42643 discipline): killed the prompt-caching angle (already expert-level/maxed), and confirmed #28053 was real, P2-open, and the prior PR was *author-abandoned* not rejected → green light.
- Caught 2 agent errors during verification (helper not actually imported; 3 sites not 2).

## Identity / hygiene
- Commit authored `Basil Al Shukaili <basilalshukaili@gmail.com>` — never dhofaroffice365/lordbasil147.
- Clean PR: only my commit vs upstream/main (branch based on fresh upstream main).

## Next
- Watch PR #43293 CI (full hermetic suite runs on Linux); fix if anything Windows-only slipped past local run.
- Respond fast to teknium review — this repo merges quickly and rewards scoped, tested diffs.
- Bench of next candidates (verified, unclaimed): untracked cache TTL-downgrade bug (run_agent.py:4620 dup `cache_control` injection downgrades 1h→5m), web_extract truncation waste (#42483), TTFB watchdog generalization (#42776). One project at a time — stay on hermes-agent.
