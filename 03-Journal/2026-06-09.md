# 2026-06-09

## Win: PR #38832 merged into NousResearch/hermes-agent

- Fix landed via PR #41717 (teknium1 rebased + added defense-in-depth `on_session_end()` clear)
- Basil listed as co-author on the commit and added to `AUTHOR_MAP` in `scripts/release.py`
- Fixes issue #38788: cross-session stale `_previous_summary` contamination in ContextCompressor
- 3 new tests (all 33 pass): stale-clear, handoff-preservation, non-interference scenarios

## System update

- Removed all DeepSeek references from `00-System/` — Claude Code is the sole toolchain now
- Model routing updated: Opus 4.8 (Brain) / Sonnet 4.6 (Worker) / Haiku 4.5 (Scout)
- Hermes-agent dossier updated with merged PR details

## Next

- Triage hermes-agent for next unclaimed P2/P3 bug — priority areas: cron, delegation, Windows CLI
- Rule: always check existing PRs before starting work (learned from issue #36771 / duplicates)
- Stay on hermes-agent until 2-3 merged PRs build maintainer rapport

---

## Session 2 — DeepSeek cleanup shipped, v0.2.1 hardening, PR sweep

### Shipped to github-rep (pushed to main)
- Removed DeepSeek from `00-System/architecture.md` (last live doc naming it); aligned to the Opus/Sonnet/Haiku ladder. Token-optimization levers preserved. (commit 468c066)
- **v0.2.1 hardening** (commit 29fa71c): honest activity signal — measures last *push* to an owned repo, not `user.updated_at` (which bumps on stars/follows/bio); HTTP 429 secondary-rate-limit handling; single repo fetch (dropped a redundant API call); best-effort cache writes; dropped unused `python-dateutil`. Added `test_api.py` + `test_cli.py`. **46 tests pass.** PyPI publish pending Basil's OK.

### Identity / attribution (important)
- All commits now `Basil Al Shukaili <basilalshukaili@gmail.com>`. NEVER `dhofaroffice365` / `lordbasil147@gmail.com`.
- **haystack #11492 CLA was blocked** because a merge commit was authored under `lordbasil147@gmail.com` → GitHub attributed it to `dhofaroffice365` (unsigned). Re-authored to basilalshukaili + force-pushed the fork → **CLA now SUCCESS**, awaiting maintainer review. (Vercel preview FAILURE is deepset-internal, non-blocking.)

### PR sweep outcomes
- agent-kit #322: posted "no changeset needed" (verified `examples/` not in pnpm workspace).
- mcp-go #902 + mcp-servers #4279: polite nudges (both all-green, 7d).
- Closed duplicate agentbroker PRs #6871 + #7285 (consolidate to #7306; still needs a Glama badge to merge — Basil action, or drop). Dropping the awesome-list spam strategy.

### ⚠️ hermes #42643 — misdiagnosis caught by review (PR NOT opened)
- A Sonnet worker produced a fully-tested fix patching `config.set`'s empty-arg case. **Wrong handler.** Bare `/reasoning` routes to `config.get` (session.ts:393 guard since Apr 16), and `config.get`'s reasoning branch (server.py:5401, since Apr 13) already returns the value cleanly. `parse_reasoning_effort("medium")` returns a valid dict → not reproducible on main.
- Action: did NOT open the PR. Posted a triage comment asking the reporter to confirm on latest + share `hermes_constants.__file__` (likely a stale/shadowed module). New lesson written.

### Next
- Watch haystack #11492 for review; nudge in ~a week if silent.
- If #42643 reporter confirms a real repro, fix the ACTUAL path; else re-triage hermes-agent for a reproducible P2/P3.
- Decide on PyPI 0.2.1 publish.